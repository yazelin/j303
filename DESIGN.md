# J303 — 設計語言 / Design System

> 林亞澤（Yaze Lin）個人 lab 的設計系統。
> 這份是「為什麼這樣設計」的單一事實來源。未來做相關設計（其他站、海報、簡報、品牌延伸）以此為準。
> Live：https://yazelin.github.io/j303/ ｜ 單檔實作：`index.html`

---

## 0. 核心精神（先讀這段）

1. **個人 maker lab，名片式，質感是硬底線。** 目標是讓人看了想「這畫面居然做得出來，我想跟這個人學」。craft 本身就是說服力。
2. **反「通用 AI 感」。** 兩個廉價元兇要永遠避開：
   - **綠→藍→紫 的霓虹漸層** = 每個 AI 新創都在用的撞臉色，是 template 味，不准用。
   - **純黑死底** = 廉價。高級的暗是「有溫度、有材質」的深色，不是 `#000`。
3. **墨 × 東方為底。** 書法、水墨、印章、宣紙、青瓷青花 —— 這是我們的母語，不是別人的。
4. **單一重點色 > 多色漸層。** 一個有個性的重點色才高級；彩虹漸層才廉價。
5. **文案是 maker 分享，不是課程推銷。** 見 §5，那是這個專案最貴的一課。

---

## 1. 雙溫度雙人格（Dual-Temperature Theming）

同一個站、兩個靈魂。不是「亮/暗」開關，是兩種**溫度與情緒**。

| | 暗色 Dark | 淺色 Light |
|---|---|---|
| 代號 | **黑鑲金**（black-gold luxe） | **青花**（blue-and-white porcelain） |
| 情緒 | 暖火餘溫、夜、貴氣、沉 | 浪花涼涼、晝、清雅、透 |
| 溫度 | 暖（紅 / 金 / 琥珀） | 冷（藍 / 深藍 / 青綠） |
| 底 | 暖墨黑 | 冷藍灰水面 |
| 名字「林亞澤」 | 香檳金白墨 | 深暗藍墨 |

**主題決定邏輯**（`ThemeToggle`）：
1. 第一次進站 → 跟系統 `prefers-color-scheme` 走。
2. 使用者手動切換後 → 存進 `localStorage('yz-theme')`，之後永遠記住手動選的。
3. 切換鈕要**明顯**（夠大、夠對比、進場閃三下 `attn` 提示可點）。發現有人不知道能切，就是鈕做得不夠顯。

---

## 2. 色彩系統 —— 最重要的一節：**角色，不是色票**

> 核心鐵則：**每個顏色綁一個「角色」。兩個主題用同一套角色邏輯，只是換色。**
> 違反這條 = 邏輯不統一 = 廉價。（這條是踩過坑才立的，見 §2.3）

### 2.1 角色 → 顏色 對照表

| 角色 | 用在哪 | 暗色 | 淺色 |
|---|---|---|---|
| **底（紙）** | 全站背景 | 暖墨黑 `#16130f` | 冷藍灰 `#eef2f6`→`#ccd6df` |
| **主文字** | lead / 內文 | 暖米白 `#ece6da` / `#c7bfb0` | 冷墨 `#19293a` / `#41505d` |
| **標題** | `.sec-h` 大標 | 米白 `#ece6da` solid | 墨藍黑 `#17293a` solid |
| **主色（結構）** | 段標、分類標題、連結、按鈕 | **朱紅** `#cf4438`／鈕底 `#bb352b` | **墨藍** `#225378`／鈕底 `#234e70` |
| **點睛（強調）** | 內文強調關鍵字 `.hl` | **金** `#d9ab57` | **青綠／青瓷** `#2f8c79` |
| **名字墨** | hero「林亞澤」 | 香檳金白 | 深暗藍 |
| **流體氛圍** | hero 背景流體、光暈、餘燼 | 暖（朱／琥珀／金） | 冷（藍墨水面） |

### 2.2 那條鐵則展開

- **結構一律用主色**：段標 `.sec-label`、分類標題 `.cat-h`、文內連結、所有按鈕（`看做法`、`訂閱`、CTA）。
- **強調一律用點睛色**：只有「內文裡要被注意的關鍵字」`.hl`。
- **名字永遠不用主色（朱紅）。** 人的名字用朱紅像印章警示、像紅字，不像名字。名字要素雅（金白 / 深藍墨），把搶眼留給重點色。
- 暗色 = 黑鑲金，所以金鋪在「字（強調字）、名字、光暈、餘燼」這些**發光/微光**的地方；紅是結構骨架。
- 淺色 = 青花，藍是骨架，青綠是同門青瓷色給冷調一點生氣。

### 2.3 踩過的坑（別重犯）

- **曾經**：名字用朱砂紅 → 像印章不像名字。**修**：暗色改香檳金白、淺色深藍墨，紅退回當重點色。
- **曾經**：淺色強調字給了青綠（第二色），暗色強調字卻還是紅、分類標題又是金 → **兩個主題角色對不上**。**修**：用 §2.1 的表把角色對齊，暗色強調字改金、分類標題改朱紅。
- 改標題顏色時，若把 `.sec-h` 的 `-webkit-background-clip:text` 拔掉，淺色 override 若還留著 gradient 會變「填滿色塊」。標題一律 solid `color`，不要殘留 gradient。

---

## 3. Hero —— 活墨字標（招式系統）

整個 hero 是一個 **WebGL 流體（Navier-Stokes，改自 PavelDoGreat 的 MIT 實作）**，名字「林亞澤」不是貼上去的字，是**用流體墨寫出來、活的**。

### 3.1 招式

- **招1 書法自書寫**：用 makemeahanzi 的筆順資料（strokes + medians），名字按真實筆順一筆一筆寫出來（`WRITE_DUR=6.5s`）。
- **招2 游標即毛筆**：滑鼠移動 = 毛筆攪墨，慢=粗、快=細（筆壓）。
- **活墨 stamp**：把寫好的筆畫遮罩「蓋印」進流體染料，每幀重蓋，名字就一直在墨裡浮動。
- **bloom 輝光**：染料模糊後加回去 → 字會發光（暗色金光、淺色藍墨）。
- **餘燼粒子**：極淡塵光緩緩上升（暗色金琥珀）。
- **招3 墨洗**：往下捲時注入向下速度，墨往下沖（`window.__wash`）。

### 3.2 雙溫度的墨（關鍵技術）

- **暗色**：`mix-blend:screen`。`nameColor()` 回香檳金白（`r=w, g=w*0.94, b=w*0.82`），`genColor()` 回「朱紅→朱橘→琥珀金→香檳金→深胭脂」光譜。發光的金白名字浮在暖火流體裡。
- **淺色（水墨）**：`#fluidCanvas { filter:invert(1); mix-blend:multiply; }`。要「深暗藍墨」就把染料調成**暖白、低藍**（`b≈0.76–0.82`），反相後就變深藍黑。純黑墨在「一缸水」般的冷紙面化開。

### 3.3 字型

- **YazeKai**：名字專用毛筆字。本機 **AR PL UKai TW**（ukai.ttc face 2）subset 成「林亞澤」三字 → woff2 → base64 內嵌（~2KB）。
- ⚠️ **Google 的毛筆字（Ma Shan Zheng / Long Cang）是簡體，缺正體「亞 / 澤」。** 正體繁中一定要用本機 UKai subset 或 makemeahanzi 筆順，別用 Google 毛筆。

### 3.4 Hero 踩過的坑

- **rAF 不受 vsync 限制**（自動化/某些環境）→ 書寫進度 `writeT` 要用**真實時間**累加（`writeT += realDt`），不要用每幀 dt；首幀 `realDt` 上限 `0.1` 否則名字瞬間寫完。
- **名字捲走後回來會不見**：名字位置是用 `.mark` 的**當下螢幕座標**蓋印的，不在頂端時若有重排（resize）就蓋到畫面外。**解**：每次真正回到頂端（`scrollY<6`）就 `__rebakeName()` 在正確位置重蓋。
- `CURL>0` 會自我放大成亂流，氛圍要柔就壓低 CURL / 提高 VEL_DISS。
- 名字要清楚：高 `DENSITY_DISS` + 每幀重蓋，不然糊掉。

---

## 4. 字體層級（Typography）

| 用途 | 字體 | 字重 / 特徵 |
|---|---|---|
| 名字字標 | `YazeKai`（毛筆）→ fallback `Noto Serif TC` | 行楷，只有「林亞澤」 |
| 大標 `.sec-h` | `Noto Serif TC` | 400–500，letter-spacing `.06em` |
| 內文 / 課程描述 | `Noto Serif TC` | 300，line-height 1.85–2 |
| 段標 / 英文小標 / 標籤 | `Space Grotesk`（mono 感） | 字級小、`letter-spacing .1–.28em`、`uppercase` |

原則：**中文走 serif（宋/楷）顯文氣，英文/標籤走 Space Grotesk 顯工程感。** 兩者並置 = 文人 × maker。

---

## 5. 文案 / Voice —— 這個專案最貴的一課

> 這一節是反覆被使用者打回來、一條一條磨出來的。寫任何 J303 相關文字前先過一遍。

### 5.1 鐵則

1. **標題不放標點符號。** 大標、段標、分類標題都不放逗號頓號括號。內文才有標點。
   - ✅「做完就有一個能用的 AI 應用」 ❌「做完，你就有一個能用的 AI」
2. **結果先講。** 標題直接寫使用者得到什麼，別繞圈鋪陳。
3. **maker 分享口吻，不是課程推銷。** 換掉「課程感」的詞：
   - 課程 → 我做的東西 / Builds｜教學 → 看做法｜教 → 分享｜開課 → 做了新東西
4. **絕不超賣。** 沒上線的別說上線、沒每天用的別說每天用。
   - 被打回的真例：「這些產品我已經做出來上線了」（AgentOS 還在開發）、「我每天在用」（其實還在開發整合）。
5. **講人話，避開 AI 行銷腔。** 這些是「AI 寫的」的破綻（tells），全部避免：
   - 破折號 `——` 到處堆疊
   - 「讓 X 不只 Y、還能 Z」句型
   - 「把 A 變成 B」句型
   - 結尾硬加一句廣告詞（「從這裡入門」「這是關鍵一步」「從這塊看板開始」）
   - 一連串頓號排比
6. **不寫道歉式句。** 「不常寄」「不會轟炸你」「隨時退訂」這種先自清的話一律刪，顯得心虛。要自信。

### 5.2 語氣定調的句子（範本）

- 自我介紹：「我做工廠自動化做了很多年，現在在做 AI，把『東西要真的能用』這個習慣帶過來。」
- About 標題：「**樂於分享**」（講性格，不是講功能）。
- 課程牆標題：「**跟我一起做 AI 應用**」（邀請感、涵蓋 agent 與一般應用）。
- 訂閱標題：「**好東西跟好朋友分享**」。

---

## 6. 版面與元件（Layout & Components）

### 6.1 結構（由上到下）

`揮毫 hero`（捲動 `--heroOp` 淡出）→ `關於我` → `Builds 課程牆` → `Projects 我做的專案` → `Subscribe 訂閱` → `頁尾`。

### 6.2 資料驅動的課程牆（重要：要能長大）

- 課程**不寫死在 HTML**。一個 `COURSES` 陣列 + `CATS` 分類陣列，`#coursesRoot` 用 JS 渲染。
- **加一門課 = 加一筆資料；加一個分類 = 在 CATS 加一筆。** 版面自己長，不會亂。
- 分類各自一塊（`.cat-block` + `.cat-h` 分類標題 + `.courses-grid`）。目前分類：`公司、工廠都能用` / `Bot Agent` / `讓 AI 會用工具`。
- Grid：`repeat(auto-fill, minmax(min(100%,460px),1fr))`。兩張並排填滿；單張時不會撐成整排（用 `auto-fill` 不是 `auto-fit`）；窄螢幕單欄。

### 6.3 元件規格

- **卡片 `.card`**：暗=`linear-gradient(165deg, rgba(255,247,235,.05), rgba(255,247,235,.012))` + 邊 `rgba(236,230,218,.1)`；淺=`rgba(255,255,255,.72)`。hover 上浮 7px + 主色邊光。
- **主按鈕（藥丸）**：實心主色（暗 `#bb352b` / 淺 `#234e70`），米白字，圓角 `999px`，柔陰影。hover `brightness(1.1)`。
- **Ghost 連結**：無底、暖灰/冷灰字，hover 轉主色。
- **訂閱表單**：圓角輸入框 + 藥丸送出鈕，雙主題各自配色（見 §7）。
- **質感層**：`#content` 上有極淡 fractalNoise 顆粒（`opacity .06–.07, mix-blend overlay`）+ 主色微暈 radial。**深色靠材質不靠發光**。

### 6.4 動態（Motion）

- 進場：名字書法自書寫；其餘元素 `IntersectionObserver` 加 `.reveal.in` 淡入上浮（`transition-delay` 依序錯開）。
- 捲動：`--heroOp = clamp(1 - scrollY/(innerHeight*0.62), 0, 1)` 控制 hero 整體淡出 + 招3 墨洗。
- 切換鈕：進場 `attn` 脈衝三下（per-theme `--attn` 色）。

---

## 7. 漏斗 / MailerLite 整合

- 訂閱區是**自刻 HTML form**（POST 到隱藏 `iframe`），**不用** MailerLite 的 JS widget（那長相套不進設計）。
  - 端點：`assets.mailerlite.com/jsonp/<account>/forms/<formId>/subscribe`
  - 欄位：`fields[email]` + hidden `ml-submit=1`、`anticsrf=true`
  - 送出後原地顯示成功訊息（`iframe onload`），不跳頁。
  - 樣式跟著雙主題走（暗 朱紅鈕 / 淺 墨藍鈕）。
- **GitHub 當第二管道**：follow 帳號 = 新 repo 進動態；star = 收藏不是通知（措辭要照實，別誤導）。
- 漏斗邏輯：hub 訂閱者走**獨立線**，不混進課程的養成 automation（課程養成假設 Day0 是 PDF 下載信，hub 沒 PDF）。hub 另開一條 automation，Day0 純文字歡迎信。
- 信末 footer 的公司地址是法律強制、拿不掉；放**公司地址**不要放住家。

---

## 8. 快速色票（複製用）

```
/* ── 暗色 黑鑲金 ── */
底        #16130f   （scene 漸層 #1c1712 → #0b0907）
主文字     #ece6da / #c7bfb0
標題       #ece6da
主色 朱紅   #cf4438   鈕底 #bb352b   連結 #d9544a
點睛 金     #d9ab57
名字       香檳金白（r=w, g=w*0.94, b=w*0.82, w≈0.90–0.95）
餘燼       226,178,108     微光 222,168,110

/* ── 淺色 青花 ── */
底        #eef2f6 → #ccd6df   （body #d4dee6, content #eaeef3）
主文字     #19293a / #41505d
標題       #17293a
主色 墨藍   #225378   鈕底 #234e70
點睛 青綠   #2f8c79
名字       深暗藍墨（染料暖白低藍 r0.95 g0.91 b0.76 → invert）
```

---

## 9. 一句話總結

**墨為底、雙溫度（暗=黑鑲金暖、淺=青花冷）、一主色撐結構、一點睛色提強調、名字永遠素雅、文案講人話不超賣、課程資料驅動能長大。** 守住這幾條，延伸到哪都不會走味。
