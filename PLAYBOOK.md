# Teaching-Repo Playbook —— 我怎麼做一套「實作營」教學 repo

這是我做「AI 應用實作營」系列時整理出的可複用方法論:**怎麼把一個技術主題,做成一個能動手、好讀、有對照、能上線教學的 starter repo**,再把多個 repo 組成系列、匯整進這個 hub。

公開分享,歡迎照著做自己的教學 repo。

---

## 1. 架構:品牌 → 系列 → 模組

```
品牌（一個）
 └─ 系列（一個主題，2–4 個模組）
     └─ 模組（= 一個 starter repo）
Hub 頁面把所有系列與模組整理成一頁，每個模組連到它的教學頁。
```

- 單位是「**系列**」不是「單一 repo」:一場分享 = 一個系列(數個模組)。
- 系列要**主題一致、範圍不大**(2–4 模組)。「範圍太大」看的是主題一不一致,不是模組數量。

---

## 2. 單一模組 repo 的骨架

```
<name>-starter/
  主程式 / app / skill        # 真能跑的東西
  baseline / part1            # Part 1 手刻/baseline（對照組）
  docs/00-08.md               # 課綱
  tests/ + smoke 測試          # 免 API key、確定性
  tutorial.html               # 教學頁（精簡快速了解版）
  index.html                  # landing
  README.md  DESIGN.md
  assets/banner.svg
  pyproject.toml              # uv 專案
  .github/workflows/ci.yml
```

**docs 課綱(9 章)**:00 總覽 / 01 快速開始 / 02 架構 / 03 逐步實作 / 04 部署 / 05 常見踩雷 / 06 改造成你的場景 / 07 延伸 / 08 對照課。
01 與 03 走「指令 → 真實輸出 → 成功長這樣 → 章末練習」的帶走式,不是指路式。

---

## 3. 教法核心:Part 1 → Part 2 對照

每個模組分兩段,讓學員體會「同樣的事可以多簡單 / 更好 / 更真實」:

- **Part 1**:從零手刻 / 誠實 baseline —— 看懂底層在做什麼。
- **Part 2**:換上真工具、框架或真實資料 —— 拿生產力。

例如:手刻 stdio JSON-RPC → FastMCP;手刻簽章/Reply → 官方 SDK;關鍵字檢索 → 語意檢索;模擬資料 → 真實 MQTT;每次貼 prompt → 打包成 Skill。

做法:Part 2 的新相依用 **optional extra 隔離**,base 安裝不變;框架的真實 API **先實測過再寫進教材**(版本會變,不能憑記憶)。

---

## 4. 真實案例階梯

每個模組可以連一個「真實案例」:學的東西在真產品裡長什麼樣(teaching starter → 真實專案 → 企業平台)。再附社群的 awesome 清單,讓學完能去挖更多。加之前一定先確認連結存在、內容對得上。

---

## 5. 環境與測試

- **uv**(`uv sync` / `uv run`),環境章節含雙平台。
- **免 API key 的確定性測試**:本地假 server、in-process broker、framework 的 test model、結構驗證 + 跑腳本斷言。一支 `client_smoke_test.py` 跑全部,輸出要**確定性**(這樣文件可以逐字引用它)。
- CI:`uv sync` → 跑 smoke;有多軸就用 matrix。

---

## 6. tutorial.html 設計系統(精簡快速了解版)

教學頁不要做成「把 docs 全文塞成一條長龍」,改成精簡頁:

- **五塊**:這是什麼 → 你會做出什麼 → 5 分鐘跑起來(真實輸出)→ Part 2 對照 → 下一步。
- **完整章節清單**:列 9 章,各一鍵連到 GitHub 上的 docs 全文(內容不遺失,只是移出網頁)。
- **暗/淺雙主題**:右上切換鈕 + `localStorage` 記憶 + **淺色預設**;每個 repo 一個品牌色,中性 token 共用。
- **頂條**:滿版不透明 sticky header(內容置中對齊內文欄);章節導覽不黏,避免兩層 sticky 疊影與半透明鬼影;section 加 `scroll-margin-top` 讓錨點不被頂條蓋。
- **鐵則**:不用 emoji、不用圈圈數字 ①②③(用純數字;箭頭 ← ↓ → 可);**指令輸出一律來自真實跑,不可造假**。

最快做法:挑一個定稿的 tutorial.html 當模板,只換品牌色與內容。

---

## 7. Hub 聚合(系列 → 模組)

Hub 頁用一個資料陣列描述「系列 → 模組」,渲染成每系列一個區塊(抬頭 + 一句 tagline)+ 模組卡格線。模組卡 = banner + 標題 + Part1→Part2 一句話 + 連結(教學頁 / 原始碼 / 真實案例)。籌備中的模組用「coming soon」卡先佔位,做好再翻成真模組。

---

## 8. 製作流程

1. **先設計再動工**(設計沒定稿不寫 code;視覺先做樣板截圖確認)。
2. **先做一個樣板**,定稿後再**批次**其餘(每個 repo:實作 → 對抗式檢查不造假 → 修)。
3. 新 repo 的功能核心先**自己寫 + 實跑驗證**,再生成文件(文件描述的必須是真能跑的程式)。
4. PR → CI 綠 → squash merge(trunk-based、短命 branch)。

**踩過的坑**:重構後舊文件用詞沒同步(要 grep 全部 docs 找 stale 字串);圈圈數字被當類 emoji(全系列統一純數字);smoke 輸出在終端機 vs pipe 順序不同(用 capture 依序印)。

---

## 鐵則總表

- 程式碼不能錯、文件不造假(輸出一律真實跑出來的)。
- 無 emoji、無圈圈數字。
- uv;測試免 key + 確定性。
- 網頁 = 快速了解;完整細節在下載手冊與 docs。
- 先做一個樣板給人看,定稿才批次。
- 真實案例 / awesome 連結加之前先驗證存在。

---

*這份是公開方法論版。實際的內容站見本 repo 的首頁(`index.html`)。*
